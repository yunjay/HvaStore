""" graphql needed in both yoga and prisma"""

type Item
type Mutation{
    createItem(title:String,description:String,image: String,
  largeimage: String,
  price: Int,
  createAt: DateTime,
  updatedAt: DateTime,) :
  Item!
}

type Query{

}
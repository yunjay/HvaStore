"""느낌표는 필수 필드"""

type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  largeimage: String
  price: Int!
  createAt: DateTime!
  updatedAt: DateTime!
  #user: User!
} 
